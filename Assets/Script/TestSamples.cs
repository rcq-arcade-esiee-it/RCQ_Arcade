using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestSamples : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i < 5555; i++)
        {
            print("hello Unity");
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
